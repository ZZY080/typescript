interface A {
  fn: (value: number | string) => string;
}

// interface B extends A {
//     fn:(value:string)=>string
// }

// type TableListItem = {
//   key: number;
//   name: string;
//   createdAt: string;
// };

// const columns: TableListItem[] = [];

type P = {
  name: string;
  age: number;
  address: string;
};


